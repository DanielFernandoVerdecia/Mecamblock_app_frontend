<<<<<<< HEAD
# 🧑‍💻 Pendientes!!! 📋❌✅


## 1. Definir correctamente la estructura de los formularios que vamos a usar. Por ejemplo si vamos a registrar Bloques necesitamos:

### * Id
### * Tipo
### * Color
### y así sucesivamente .....


## 2. Tener bocetos sobre el diseño de los formularios, estructura de la página, esqueleto:

### Esto ayudará a que los programadores del Frontend ya tengan mejor noción sobre lo que tienen que programar directamente


## 3. Buscar más aspectos, características de la funcionalidad que vamos a programar para Mecamblock:

### Veo que por el momento tenemos planeado hacer muchos CRUD, hay que tener mucho cuidado porque la profesora solo acepta hacer CRUD sencillo como solo una funcionalidad, hay que pensar qué otras cosas vamos a agregar

  

=======
# sv

Everything you need to build a Svelte project, powered by [`sv`](https://github.com/sveltejs/cli).

## Creating a project

If you're seeing this, you've probably already done this step. Congrats!

```sh
# create a new project in the current directory
npx sv create

# create a new project in my-app
npx sv create my-app
```

## Developing

Once you've created a project and installed dependencies with `npm install` (or `pnpm install` or `yarn`), start a development server:

```sh
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open
```

## Building

To create a production version of your app:

```sh
npm run build
```

You can preview the production build with `npm run preview`.

> To deploy your app, you may need to install an [adapter](https://svelte.dev/docs/kit/adapters) for your target environment.
>>>>>>> 2f855e7 (usamos ahora SvelteKit)
